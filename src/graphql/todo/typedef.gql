type Geo {
  lat: Float!
  lng: Float!
}

type Address {
  id: ID!
  street: String!
  suite: String!
  city: String!
  zipcode: String!
  geo: Geo!
}

type User {
  id: ID!
  name: String!
  username: String!
  email: String!
  address: ClientAddress!
}

type Todo {
  id: ID!
  title: String!
  user: User
}

type Query {
  todos: [Todos!]!
  todo(id: ID!): Todo!
  users: [ClientUser!]!
}